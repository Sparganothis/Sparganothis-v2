use dioxus::prelude::*;

#[component]
pub fn GlobalChatPage() -> Element {
    // ! EMPTY: the content is filled in by the global chat overlay component to preserve
    // ! the history of the chat room while clicking
    rsx! {    }
}
